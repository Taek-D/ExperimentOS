# FIGMA-EXPORT.md — ExperimentOS UI Handoff

Generated by ui-pipeline on 2026-03-23.

## Design System Tokens

See `MASTER.md` in project root for complete design system specification.

### Quick Reference
- Primary: `#00e5a0` | Background: `#080c0a` | Surface: `#0c1310`
- Font: Inter (heading 700-800, body 400) + JetBrains Mono (data)
- Border Radius: 12px (default), 16px (cards), 9999px (pills)
- Motion: 150ms (short), 250ms (medium), cubic-bezier(0.25, 0.1, 0.25, 1)

## Screens

### 1. Landing Page (`components/LandingPage.tsx`)
**Sections:** Hero / Stats Bar / Features (6 cards) / How it Works (3 steps) / CTA / Footer

**Figma Notes:**
- Hero has grid-pattern background + radial glow effect
- Dashboard preview card uses glassmorphism (blur 12px, 4% white bg)
- Feature cards have top-border glow on hover (gradient from transparent to primary/30)
- Stats use JetBrains Mono bold for numbers
- CTA card has gradient border + top glow blur

### 2. Dashboard (`components/Dashboard.tsx` + `App.tsx`)
**States:** Upload/Empty / Loading / Results (4 tabs)

**Figma Notes:**
- Top bar: 56px, backdrop-blur-xl, logo with hover glow effect
- Stats cards: 3-column grid, background icon at 4% opacity, hover reveals primary gradient
- Tab bar: bottom-border indicator (2px primary), icon + label
- Metrics table: sticky header, mono font for data, status badges with color coding
- Critical rows: left 2px red accent line with gradient fade

## Copy to Figma Options

### Option A: Standard Mode
1. Run the dev server: `npm run dev`
2. Open in browser at `http://localhost:3000`
3. Use Figma's "html.to.design" plugin to import live page

### Option B: Static HTML
1. Build: `npm run build`
2. Open `dist/index.html` in browser
3. Use html.to.design plugin or manual screenshot

## Stitch Prompts (for regeneration)

### Landing Page
```
ExperimentOS Landing Page — A/B Test Decision Platform.
Hero with "Ship with Confidence" headline, electric mint (#00e5a0) CTA on dark bg (#080c0a).
Stats bar, 6 feature cards with glassmorphism, 3-step workflow, CTA section.
Inter font, JetBrains Mono for data. Premium dark SaaS aesthetic.
```

### Dashboard
```
ExperimentOS Dashboard — A/B test results view.
Top bar with logo, tab navigation. 3 stats cards (Lift, P-Value, Guardrails).
Full metrics table with status badges. Forest plot area.
Dark theme (#080c0a), electric mint (#00e5a0) primary. Data-dense professional layout.
```
